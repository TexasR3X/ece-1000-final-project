{
  "name": "ece-1000-final-project",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Austin Dalley",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "start": "electron .",
    "build": "tsc",
    "dev": "npm run build && cd ./frontend/ && npm run build && cd ../ && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "electron": "^41.0.2",
    "typescript": "^6.0.2"
  },
  "overrides": {
    "yauzl": "^3.2.1"
  }
}
